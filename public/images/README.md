# Image Asset Folders

Drop generated or production-ready image assets into these folders. The React app already references the paths documented in the root README and includes CSS fallbacks when the files are absent.
