# Image Asset Folders

Drop generated or production-ready image assets into these folders. The React app uses local SVG placeholders for the logo, merch, drinks, stage and show imagery so it builds without external image URLs.
